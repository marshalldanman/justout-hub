# === JUSTOUT-HUB HANDOFF — 2026-05-02 ===
# Paste this entire block into your local Claude Code session.
# It bridges the cloud session's work with your local environment.

## WHAT JUST HAPPENED (cloud session)

The justout-hub monorepo was restructured. Three separate repos were merged
with full commit history into the hub:
- fpcs-dashboard → dashboard/
- fpcs-helpdesk  → helpdesk/
- fpcs-bots      → bots-hq/

A critical bug was found and fixed: Token Master login was broken because
it used `#main-content` instead of `#dashContent`, so auth.js never
initialized and the Google sign-in button was dead. Fixed on branch
`claude/task-appraisal-dashboard-eaigR` — needs merge to master.

service-account-key.json was verified safe (gitignored, never in history).
Gegenkraft tasks moved to ON HOLD per Commander directive.

## REPO LAYOUT (read CLAUDE.md for full conventions)

justout-hub/
├── CLAUDE.md              ← repo rules, agent hierarchy, task format
├── LIFE.md                ← Project of Life (priorities, scorecards)
├── tasks/                 ← task inventory (APPRAISAL_MASTER.txt + JOH-*.txt)
│   └── cloudflare-setup.md ← updated with redirect rules + Firebase auth notes
├── dashboard/             ← TRAILS dashboard (Firebase Auth, Firestore)
│   ├── js/auth.js         ← 3-tier auth (admin/member/guest), Firebase config
│   ├── js/token-master.js ← token usage command center
│   ├── token-master.html  ← JUST FIXED: now uses #dashContent
│   └── ...                ← bots.html, sentrylion.html, income.html, etc.
├── helpdesk/              ← issue tracker (merged from fpcs-helpdesk)
├── bots-hq/               ← bot HQ landing (merged from fpcs-bots)
├── gegenkraft/            ← habit tracking (ON HOLD)
├── living-journal/        ← personal memory archive (see AI-CHARTER.md)
├── JapHQ/                 ← voice command center
└── .mcp.json              ← MCP server config (google-sheets, playwright, memory)

## YOUR TASKS (in priority order)

### 1. MERGE THE BRANCH (git)
```
cd C:\Users\James\AI_Projects\justout-hub
git fetch origin claude/task-appraisal-dashboard-eaigR
git checkout master
git merge origin/claude/task-appraisal-dashboard-eaigR
git push origin master
```

### 2. FIREBASE: Add hub.justout.today to authorized domains
- Go to: https://console.firebase.google.com/project/fpcs-dashboard-63b25/authentication/settings
- Under "Authorized domains", add: hub.justout.today
- Keep: fpcs-dashboard-63b25.firebaseapp.com, dashboard.justout.today (transition)
- This is REQUIRED for Token Master and all dashboard Google sign-in to work

### 3. CLOUDFLARE: Set up redirect rules
Login to Cloudflare dashboard for justout.today zone.
- Rules > Redirect Rules > Create Rule:
  - Rule 1: hostname = dashboard.justout.today → 301 to https://hub.justout.today/dashboard/${URI}
  - Rule 2: hostname = helpdesk.justout.today → 301 to https://hub.justout.today/helpdesk/${URI}
- DNS > Records: remove old CNAME records for "dashboard" and "helpdesk" subdomains
  (they pointed to separate Firebase Hosting sites that no longer exist)

### 4. FIX justout.today APEX 503
The bare domain justout.today returns 503 with TLS error. It's resolving to
AWS Global Accelerator IPs (GoDaddy forwarding). Options:
  a) In GoDaddy: remove forwarding, point nameservers to Cloudflare
  b) OR in Cloudflare: add A records for GitHub Pages:
     185.199.108.153, 185.199.109.153, 185.199.110.153, 185.199.111.153

### 5. TAX PIPELINE AUDIT (from LIFE.md priorities)
Open the Tax HQ page (dashboard/index.html or the live site).
For each year 2022-2025, determine: filed / in-prep / not-started.
Report back a one-line summary per year.

### 6. SIM SWAP PROTOCOL (from LIFE.md priorities)
Check tasks/ folder for the Dark Twin Detector task (JOH-P1-003 area).
The 7 SIM swap protection steps need to be marked done/not for Commander's lines.

### 7. TWITCH RECORDER VERDICT
Look for twitch-recorder/ directory in the repo.
Either write one sentence of purpose, or `git rm -r twitch-recorder/`.

## KEY FILES TO READ FIRST
1. CLAUDE.md — all repo rules
2. LIFE.md — priorities and scorecards
3. tasks/APPRAISAL_MASTER.txt — full task inventory
4. tasks/cloudflare-setup.md — infrastructure setup notes

## FIREBASE CONFIG (for reference, already in auth.js)
- Project: fpcs-dashboard-63b25
- Auth domain: fpcs-dashboard-63b25.firebaseapp.com
- API key: AIzaSyBgYAPO_0cEvhPzfU6GZykJVUnF55LEzXQ (public client key, safe)

## NOTES
- The Gegenkraft tasks are ON HOLD — do not work on them
- living-journal/ has strict rules — read AI-CHARTER.md before any writes
- The mp4 at repo root is legacy bloat (~74MB) — needs to be moved to Drive
- Playwright MCP is available for browser automation if needed
- Google Sheets MCP is available for sheet operations
